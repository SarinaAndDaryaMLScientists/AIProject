

class FinalMinMaxAlgorithmForHiveGame():
    def huristicValue(self,game_page, move_count):
        for i in range(8):
            for j in range(8):
                for a in game_page[i][j]:
                    if a.bee_type == 'Q' and a.color == 'yellow':
                        pass
                        # for c in neighborPoints

    def minmax(self, game_page, move_count, turn):
        pass

